# 键鼠同步.exe 反编译报告

## 工具

- 已安装 Ghidra：`C:\Users\user\tools\ghidra_12.0.4_PUBLIC`
- 使用 JDK：`C:\Program Files\Zulu\zulu-21`
- Ghidra 工程：`C:\Users\user\ghidra_work\keymouse_sync\project\KeyMouseSyncProject`

## 输出

- 原始 Ghidra 批量反编译：`reverse_output\键鼠同步.decompiled.c`
- 带语义命名的副本：`reverse_output\键鼠同步.decompiled.annotated.c`
- 导出脚本：`ghidra_scripts\ExportDecompiledC.java`

Ghidra 共导出 4567 个函数，其中 2 个函数反编译失败。失败和部分 `halt_baddata()` 主要来自静态编译运行库、跳转表或数据区误识别，不影响对主程序窗口逻辑和大漠插件调用链的初步分析。

## 已确认的关键函数命名

- `app_validate_dm_config_or_warn`：检查 dm 路径、mouse、keypad、mode，缺失时弹出提示。
- `app_toggle_sync_clicked`：开启/关闭同步入口。
- `app_delete_sync_window_clicked`：删除同步窗口入口，同步中会提示先关闭。
- `app_init_dm_dll_choices`：初始化 `dm6.1544.dll`、`dm5.1423.dll`、`dm3.1233.dll` 选项。
- `app_load_config_to_controls`：从 `配置.ini` 加载配置到界面控件。
- `app_save_config_from_controls`：保存界面配置到 `配置.ini`。
- `app_add_sync_window_or_warn_duplicate`：添加同步窗口并处理重复窗口提示。
- `dm_BindWindow_wrapper` / `dm_BindWindowEx_wrapper`：大漠绑定接口包装。
- `dm_EnableKeypadSync_wrapper` / `dm_EnableMouseSync_wrapper`：键盘/鼠标同步接口包装。

## 可信边界

该程序是 32 位原生 PE，特征符合易语言/黑月静态编译，并嵌入 `YunDm.fne`、`krnln.fne` 和大漠插件辅助库信息。Ghidra 能恢复 C-like 伪代码，但不能恢复原始易语言工程、控件事件源码、中文变量名或原始模块结构。`annotated` 文件中的函数名是基于字符串交叉引用和大漠方法表推断出来的，不是原始符号。
